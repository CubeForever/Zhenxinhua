{
  "name": "truth-or-dare",
  "version": "1.0.0",
  "description": "真心话互动网站 - Netlify 版本",
  "scripts": {
    "build": "npm run copy-files",
    "copy-files": "mkdir -p dist && cp -r public/* dist/ 2>/null || xcopy public\\* dist\\ /E /I /Y",
    "dev": "netlify dev"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mongoose": "^8.0.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
